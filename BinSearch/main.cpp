#include <iostream>
#include <fstream>
#include <vector>

using namespace std;

int main() {
    int n, m;
    bool first = true;
    int pos1, pos2;
    vector<int> numbers, commands;
    ifstream fin("bstsimple.in");
    ofstream fout("bstsimple.out");

    for (int i = 1; i<=n; i++){
        if (first){
            pos1 = i;
            fi
        }
    }



    fin.close();
    fout.close;
}